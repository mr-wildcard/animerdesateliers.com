---
const { title, description } = Astro.props;
---

<style>
  .description {
    margin-left: calc(var(--spacing) * 6 + 40px);
  }
</style>

<div class="flex items-center">
  <span class="mr-6" role="presentation">
    <slot name="icon" />
  </span>
  <p class="font-bold text-lg">{title}</p>
</div>

{description && <p class="description text-lg xl:text-sm">{description}</p>}
